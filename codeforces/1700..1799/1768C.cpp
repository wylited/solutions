#include <bits/stdc++.h>
using namespace std;

void solve() {
  int n, v;
  set<int> p;
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> v;
    p.push_back(v);
  }
}

int t;

int main() {
  cin >> t;
  while (t--)
    solve();
  return 0;
}
