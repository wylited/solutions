#include <bits/stdc++.h>

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(0);

  int t;
  std::cin >> t;
  for(int i = 0; i < t; i++){
    int points;
    std::cin >> points;

    std::tuple<int, int> seq[points];
    for(int i = 0; i < points; i++){

    }


  }

  return 0;
}
