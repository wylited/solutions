#include <bits/stdc++.h>

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(0);

  int t;
  std::cin >> t;

  for(int i = 0; i < t; i++) {
    long n, k;
    std::cin >> n, k;

    char s[n];
    std::cin >> s;

  }

  return 0;
}
